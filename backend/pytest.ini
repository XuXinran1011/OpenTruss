[pytest]
# Pytest 配置
timeout = 60
timeout_method = thread
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# pytest-asyncio 配置
asyncio_default_fixture_loop_scope = function

# 覆盖率配置（可选，需要pytest-cov）
# addopts = 
#     --cov=app
#     --cov-report=term-missing
#     --cov-report=html
#     --cov-report=xml
#     --cov-fail-under=80
#     --maxfail=5
#     -v

