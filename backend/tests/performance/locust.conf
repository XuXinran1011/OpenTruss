# Locust配置文件

[locust]
# 测试主机（可以通过命令行覆盖）
host = http://localhost:8000

# Web UI端口
web-host = 0.0.0.0
web-port = 8089

# 日志配置
loglevel = INFO
logfile = locust.log

# 其他选项
headless = false
# users = 10
# spawn-rate = 2
# run-time = 60s

