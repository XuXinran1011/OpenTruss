Product Requirements Document: OpenTruss
Version: 4.0 (Final Architecture)
Status: Approved for Development
Project Type: Open Source Commercial Middleware
1. Executive Summary (项目概述)
OpenTruss 是一个面向建筑施工行业的 生成式 BIM 中间件 (Generative BIM Middleware)。
它致力于解决 CAD-to-BIM 逆向重构 中的“最后一公里”问题。通过接收上游 Agentic AI 对施工图（DWG）的非结构化识别结果，OpenTruss 在底层利用 LPG (Memgraph) + RDF 双模架构构建严谨的工程逻辑。通过 HITL (Human-in-the-Loop) 工作台，工程师在一个符合 GB50300 国标 的层级体系下进行数据清洗、参数补全与合规性审查，最终编译生成高精度的 IFC 模型。
核心理念： "Graph First, Geometry Generated"（图逻辑优先，几何由生成而来）。
2. Strategic Positioning & Architecture (战略定位与架构)
2.1. LPG + RDF Dual-Mode Architecture (双模架构)
OpenTruss 融合了两种图技术的优势，确保数据既能“跑得快”又能“说得对”：
Runtime Layer (LPG - 属性图):
技术: Memgraph (Cypher)。
作用: 高性能计算与状态管理。存储构件的几何属性、实时状态（Draft/Verified）、置信度以及 2D 拓扑连接。
场景: 工程师在前端拖拽墙体节点时，LPG 负责毫秒级更新拓扑关系。
Semantic Layer (RDF - 资源描述框架):
技术: Ontology Mapping (本体映射)。
作用: 语义标准化。LPG 中的节点标签和关系严格映射 RDF 本体（如 bot:Building, ifc:Wall），确保未来可对接 Knowledge Graph 或导出 IFCOWL。
2.2. Data Hierarchy (数据层级 - 严守 GB50300)
数据结构不再是扁平的几何堆砌，而是严格依附于工程质量验收管理树：
项目 (Project): 整个地块开发项目。
单体 (Unit Project / Building): 独立的建筑物（如：1#楼）。
分部 (Division): 按专业性质划分（如：主体结构）。
子分部 (Sub-division): 按材料或工序划分（如：砌体结构）。
分项 (Item): 按工种/材料划分（如：填充墙砌体）。
检验批 (Inspection Lot): 核心验收单元。通常是“分项”与“空间（楼层/区域）”的交集。
构件 (Element): 物理实体（如：ID 为 WALL-101 的那堵墙）。
3. User Personas (用户画像与职责)
分部工程师 (The Editor)
职责: 数据清洗 (Trace & Lift)。
动作: 修正 AI 识别的拓扑错误，补全 Z 轴高度，将“未分配”的构件归类到正确的“分项工程”中。
专业负责人/总工 (The Approver)
职责: 规则定义 (Rule Definition) & 审批 (Approval)。
关键动作: 定义检验批划分规则。决定“填充墙砌体”分项是按“楼层”划分检验批，还是按“施工段（Zone）”划分。
项目经理 (The PM)
职责: 进度监控 & 熔断 (Oversight)。
动作: 监控各分部的验收进度，拥有“一键驳回”整个楼层或分部状态的权限。
4. Functional Requirements (功能需求)
4.1. Ingestion Layer (宽进严出)
API: POST /ingest
Schema: 参照 Speckle Objects 进行扁平化适配。
策略:
宽进: 允许 height, material, inspection_lot_id 为空。
暂存: 无法确定归属的构件，挂载在 Unassigned Item 下。
4.2. HITL Workbench (智能审阅工作台)

技术栈: Next.js, Tailwind CSS (v4), D3.js / Canvas。

视觉风格: Arch-Tech Precision (建筑科技精密风)
- 底色: 浅灰 (bg-zinc-50)
- 交互元素: 墨水黑 (bg-zinc-900)
- 警示/强调: 工程橙 (text-orange-600)
- 字体: JetBrains Mono (等宽字体，代码/ID 显示)

核心模块:

Hierarchy Tree: 左侧导航树展示 Building -> ... -> Item 结构。

Triage Queue (分诊队列): 按问题严重性分组显示待处理构件
- 优先级排序: Topology Error (拓扑错误) > Z-Missing (缺失高度) > Low Confidence (低置信度)
- 位置: 左侧边栏顶部或独立面板
- 功能: 快速定位需要优先处理的问题构件

Trace Mode (描图/拓扑修正):
- DWG 底图叠加: 支持加载原始 DWG 施工图作为底图参考
- 透明度滑块 (Opacity Slider): 调整底图透明度 (0-100%)，便于对比 AI 识别结果
- X-Ray 模式: 同时显示 AI 识别线（彩色）与底图（灰度），直观对比差异
- 磁吸操作: 拖拽构件节点时自动吸附到相邻节点，修复 2D 拓扑闭合
- 技术实现: D3.js 处理拓扑关系，Canvas 渲染高性能图形

Lift Mode (升维/参数补全):
- Z-Missing 识别: 自动识别并高亮缺少高度参数的构件（紫色虚线）
- 参数面板: 提供批量设置界面，支持统一设置 Base Offset 和 Height
- 批量操作: 支持框选多个构件，一次性设置 Z 轴参数
- 预览功能: 实时预览 3D 升维效果（可选）

Classify Mode (归类模式):
- 拖拽归类: 将构件拖拽到左侧层级树的目标分项
- 批量归类: 支持多选构件批量归类
- 视觉反馈: 目标分项高亮显示，无效目标显示错误提示
4.3. Inspection Lot Management (检验批管理 - 核心新增)
这是连接“物理构件”与“管理验收”的关键环节。
发起人: 专业负责人 (Approver)。
交互流程:
策略配置: Approver 选择一个“分项”（如：填充墙砌体）。
规则定义: Approver 设定划分规则，例如：“按 Level 自动拆分” 或 “按 Zone A/B 拆分”。
自动聚合: 系统根据规则，自动创建 Inspection Lot 节点，并将符合条件的 Element 挂载到对应的 Inspection Lot 下。
人工微调: Approver 可手动将个别特殊构件移出/移入检验批。
锁定: 检验批创建后，其包含的构件状态被锁定，进入审批流。
4.4. Approval Workflow (审批工作流)
流转对象: 检验批 (Inspection Lot)（而非单个构件）。
状态机: PLANNING (规划中) -> IN_PROGRESS (清洗中) -> SUBMITTED (待审批) -> APPROVED (已验收) -> PUBLISHED (已发布)。
阻断逻辑:
只有当检验批内的所有构件都具备完整几何信息（高度、材质、闭合拓扑）时，才允许从 IN_PROGRESS 变为 SUBMITTED。
5. Technical Architecture & Schema
5.1. Hierarchy Schema (LPG Nodes)
graph TD
    Project((:Project)) -->|1:N| Building((:Building))
    Building -->|1:N| Div((:Division))
    Div -->|1:N| SubDiv((:SubDivision))
    SubDiv -->|1:N| Item((:Item))
    
    %% 核心逻辑：检验批是 分项 与 空间 的交集
    Item -->|1:N| Lot((:InspectionLot))
    
    Lot -->|1:N| Element[Element: Wall]
    
    %% 空间维度 (Approver 决策依据)
    Element -.->|Located At| Level[Level: F1]
    Element -.->|Located At| Zone[Zone: A区]


5.2. Data Models (Backend Definition)
class InspectionLotNode(BaseModel):
    id: str
    name: str  # e.g. "1#楼F1层填充墙砌体检验批"
    item_id: str
    spatial_scope: str # e.g. "Level:F1"
    status: Literal["PLANNING", "IN_PROGRESS", "SUBMITTED", "APPROVED", "PUBLISHED"]
    # Label: :InspectionLot

class ElementNode(BaseModel):
    id: str
    speckle_type: str
    geometry: Geometry  # 3D 原生几何数据（Line/Polyline），坐标格式：[[x, y, z], ...]
    # 3D Lift Params (Optional in Draft)
    height: Optional[float]
    base_offset: Optional[float]
    
    # 归属关系
    inspection_lot_id: Optional[str] # 最终归属
    level_id: str


6. Roadmap (实施路线图)

**注意**：本文档中的 Phase 编号指 **OpenTruss 项目 Phase**（项目整体开发阶段）。规则引擎功能的增量开发阶段使用 **"规则引擎 Phase 1-4"** 编号，详见 [规则引擎架构文档](../rules/RULE_ENGINE.md)。

**OpenTruss Phase 1**: Foundation & Hierarchy (基础架构)
[x] 搭建 Memgraph/FastAPI。
[ ] 实现 GB50300 六级节点 Schema (Project to InspectionLot)。

**OpenTruss Phase 2**: Ingestion & Editor (数据清洗)
[x] 实现 Ingestion API (Speckle 适配)。
[x] 开发 HITL Workbench (Trace & Lift 功能)。
[x] 实现构件的"归类"功能（拖拽分配分项）。
[x] 实施规则引擎 Phase 1（语义校验）：防止违反常识的连接。

**OpenTruss Phase 3**: The Approver's Tool (检验批策划)
[x] 开发 Lot Strategy UI: 允许负责人按楼层/区域批量创建检验批。
[x] 实现"规则引擎"（检验批划分）：IF Element.level == 'F1' AND Element.type == 'Wall' THEN Assign To Lot_001。
[x] 实施规则引擎 Phase 2（构造校验）：角度吸附、Z轴完整性检查。
[x] 实施规则引擎 Phase 4（拓扑校验）：确保系统逻辑闭环。

**OpenTruss Phase 4**: Workflow & Export (交付)
[x] 实现基于检验批的审批状态机。
[x] 集成 ifcopenshell 编译器，按检验批导出 IFC。
[x] 实施规则引擎 Phase 3（空间校验）：物理碰撞检测（可选，高级特性）。
7. Success Metrics (成功指标)
合规性: 生成的数据结构 100% 对应 GB50300 验收表格，可直接用于打印报验单。
策划效率: 负责人能在 5 分钟内完成一栋单体楼（30层）的所有检验批划分规则配置。
闭环验证: 任何 APPROVED 状态的检验批，均能无错误地导出为 IFC 模型并被 Revit/Navisworks 读取。
8. UI/UX Design Specifications (设计规范)

布局: IDE 式布局 (左侧资源/分诊，中间无限画布，右侧参数修正，顶部悬浮工具栏)。

色彩体系:

Canvas: bg-white + Dot Grid。

Surface: bg-zinc-50 (侧边栏/面板)。

Action: bg-zinc-900 (墨水黑，主要按钮和交互元素)。

Highlight: text-orange-600 (工程橙，3D 升维/强调/警示)。

字体: JetBrains Mono (等宽字体，用于构件 ID、代码显示)。

状态可视化:

🔴 Error: text-red-600 (必须处理)。

🟣 Z-Missing: text-violet-600 + 虚线 (必须补全)。

🟠 Warning: text-amber-600 (建议检查)。

🟢 Verified: text-emerald-600 (已就绪)。